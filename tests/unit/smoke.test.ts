// Sprint 2 占位测试 - 确保 vitest 配置正确
// 真实测试将在 Sprint 5+ 添加
import { describe, it, expect } from 'vitest';

describe('sprint 2', () => {
  it('vitest is configured', () => {
    expect(1 + 1).toBe(2);
  });
});
